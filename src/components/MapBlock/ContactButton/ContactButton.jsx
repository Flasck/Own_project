import React from 'react'
import { Button } from '../../Button/Button'
import styles from './ContactButton.module.css'

export const ContactButton = () => {
	return <Button className={styles.root}>Связаться с нами</Button>
}
